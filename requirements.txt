# Runtime dependencies for the serverless API (see backend/requirements.txt
# for the full local set, which also includes uvicorn and test tooling).
fastapi>=0.110.0
pydantic>=2.6.4
pydantic-settings>=2.2.1
python-dotenv>=1.0.1
httpx>=0.27.0
shapely>=2.0.3
pyproj>=3.6.1
osm2geojson>=0.2.4
